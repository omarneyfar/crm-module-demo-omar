# PostgreSQL connection — matches the credentials in docker-compose.yaml.
# Copy this file to `.env` before running the backend:  cp .env.example .env
DATABASE_URL="postgresql://crm:crm@localhost:5432/crm_db?schema=public"
