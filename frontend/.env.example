# Base URL of the CRM backend. Used server-side by the Next app
# (Server Actions / Server Components), so no CORS is involved.
# Copy this file to `.env.local`:  cp .env.example .env.local
NEXT_PUBLIC_API_URL=http://localhost:3000
